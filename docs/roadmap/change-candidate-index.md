# Change Candidate Index

Evidence index of candidate changes for the next SDD change (candidate to be named, e.g. BRE-013). This is an **evidence artifact**, not a decision: it does not prioritize, recommend, or open any change. The roadmap (or the user) selects which candidate becomes the next change.

Status legend:
- **Confirmed** — verified in the repository as of the last review.
- **Requires verification** — a reference exists, but `sdd-explore` must confirm it is still current.
- **Discarded** — the verification action disproved the original claim; the candidate is closed and kept for traceability.

## Candidates

| # | Candidate | Evidence Type | Evidence Status | Problem | Verification Action | Verification Result | Impact | Dependencies | Sources |
|---|---|---|---|---|---|---|---|---|---|
| A | **Rule Management UI** | Documentation | **Discarded** | No UI exists to create/reorder/administer rules | Confirm no equivalent UI is already implemented in `src/app` | **DISCARDED** — a full rule-management SPA exists and is mounted: `src/components/spa/BankRulesPage.tsx` rendered for `view === 'bank-rules'` in `src/components/app/PlaceholderView.tsx:43-45`, with `ConversationalRuleBuilder.tsx`, `AIRulesGeneratorTab.tsx`, and tests `tests/components/BankRulesPage.test.tsx`. Original claim was a false positive. | High (UX/operations) | BRE-011 and BRE-012 closed | `src/components/spa/BankRulesPage.tsx`, `src/components/app/PlaceholderView.tsx`, `src/components/learning/ConversationalRuleBuilder.tsx`, `tests/components/BankRulesPage.test.tsx` |
| B | **Fuzzy Matching as a rule condition** | Documentation + Code | **Confirmed (redefined)** | Flexible matching (regex/pattern) over descriptions as a BankRule condition | Confirm whether `fuzzy-matcher.ts` participates in the active engine pipeline (or is an orphan module) | **NOT ORPHAN, NOT A RULE CONDITION** — `src/lib/accounting/fuzzy-matcher.ts` IS used: `src/app/api/accounting-flow/audit/fuzzy-match/route.ts:6-7` (pre-filter + fuse.js match for duplicate detection), exposed to the front via `src/hooks/useFuzzyMatchAudit.ts`. It is an **audit/duplicate-detection tool**, not a ranking condition: `RuleConditionType` in `src/lib/rule-engine/types.ts` has no fuzzy operator. Candidate is reframed as "add fuzzy/regex as a BankRule condition type". | Medium/High | Stable engine contract | `src/app/api/accounting-flow/audit/fuzzy-match/route.ts`, `src/hooks/useFuzzyMatchAudit.ts`, `src/lib/accounting/fuzzy-matcher.ts`, `src/lib/accounting/fuzzy-pre-filter.ts`, `src/lib/rule-engine/types.ts` |
| C | **Rollback / Rule Simulation** | Documentation + Code | **Confirmed (gap is real)** | Evaluate effect of new rules before applying; revert errors | Confirm real dependency relationship with shadow metrics (`s7-05b`) and Policy Service (`s7-07`) | **CONFIRMED — no rule rollback exists.** `src/app/api/bank-rules/apply-all/preview/route.ts` is READ-ONLY (estimates totals), `src/app/api/learning/rules/simulate/route.ts` simulates condition matching only. No rollback/compensation of applied rules (the only `rollback` route is `api/learning/auto-assignments/[id]/rollback`, unrelated). Shadow metrics (`s7-05b`) is a read-only AuditLog aggregator; Policy Service (`src/lib/operational-policy/policy-service.ts`) has no rollback/simulation. | High (production) | Consolidated deterministic engine | `docs/adr/ADR-009-deterministic-rule-engine-contract.md`, `src/app/api/bank-rules/apply-all/preview/route.ts`, `src/app/api/learning/rules/simulate/route.ts`, `openspec/changes/s7-05b-shadow-metrics-reporting/spec.md`, `src/lib/operational-policy/policy-service.ts` |
| D | **Dedicated Rule Engine Testing** | Documentation + Code | **Confirmed (gap not significant)** | Dedicated test suite for the engine | Measure current Rule Engine coverage before asserting a gap | **CONFIRMED — coverage already extensive.** 23 rule-engine test files passed 381/381 tests in verification (incl. `src/lib/rule-engine/__tests__`, `tests/unit/rule-precedence-engine.test.ts`, `tests/adversarial-ranking-parity.test.ts`). 44 rule-engine src files; 21 inline tests + 28 rule-related test files in `tests/`. No coverage threshold configured in `vitest.config.ts`. Gap is narrow: formal coverage metrics/threshold, not a missing suite. | Medium | No critical pending feature work | `docs/architecture/rule-engine.md` (Next steps #4), `docs/history/2026-07-stabilization.md`, `vitest.config.ts`, 28 rule-related test files |
| E | **Security Finding 5.1 Production Verification** | Historical + Code | **Confirmed (status: pending deployment)** | Validate a security finding in production | Open `SECURITY_AUDIT.md` and verify the current state of finding 5.1 | **CONFIRMED — FIX IMPLEMENTED, pending deployment.** `SECURITY_AUDIT.md:60-77` marks 5.1 (hardcoded crypto secret fallback) as "FIX IMPLEMENTED - pending deployment"; `src/lib/crypto.ts` no longer has a dev/test fallback and `SESSION_SECRET` is mandatory (ADR-003). Remaining work is production verification, not code. | High (operational) | Production environment | `SECURITY_AUDIT.md` (5.1), `src/lib/crypto.ts`, `docs/adr/ADR-003-session-secret-required.md` |

## Additional validated candidates

| # | Candidate | Evidence Type | Evidence Status | Problem | Verification Action | Impact | Dependencies | Sources |
|---|---|---|---|---|---|---|---|---|
| F | **General rate limiter is not distributed** | Historical + Code | **Confirmed** — SECURITY_AUDIT finding 5.2 (MEDIUM) | `src/lib/security/rate-limiter.ts:14` uses an in-memory `Map`; general API rate limiting is per-instance (auth rate limiter uses DB persistence) | Confirm whether multi-instance deployment is a near-term requirement; if so, design a distributed limiter (DB/Redis-backed) | Medium/High (operational under multi-instance) | Multi-instance deployment decision | `SECURITY_AUDIT.md` (5.2), `src/lib/security/rate-limiter.ts`, `src/lib/rate-limiter.ts` |
| G | **DB-level wildcard `*` restriction** | Documentation | **Confirmed** — BRE-011 future item, explicitly out of scope there | Write/import barrier for `*` lives only in the shared domain/API layer (`src/lib/rule-engine/wildcard.ts`); no DB constraint in `prisma/` | Confirm a DB-level CHECK/constraint is desirable vs. the existing shared write barrier | Medium | BRE-011 (closed) | `docs/history/2026-08-wildcard-semantics.md`, `prisma/`, `src/lib/rule-engine/wildcard.ts` |
| H | **`rolePriority` role in canonical tiebreak** | Documentation | **Confirmed** — BRE-012 future item | `rolePriority` is still computed and consumed by `selectLegacyWinner` (`src/lib/services/rule-matching-engine.ts:347-351`) but is NOT a ranking signal in the canonical path (BRE-012 D1); decision pending on whether it stays no-oped or is removed | Decide whether `rolePriority` participates in the canonical tiebreak or is dropped (params today no-oped in Legacy) | Low/Medium | BRE-012 (closed) | `docs/history/2026-08-ranking-semantics.md`, `src/lib/services/rule-matching-engine.ts:347-351` |
| I | **Rule lifecycle: testing → active promotion** | Documentation | **Requires verification** | ADR-009 open question: automatic transition Testing→Active after N days without false positives; `rule.isActive` is the only state (`src/lib/services/rule-precedence-engine.ts:32,104`) | Confirm whether rule lifecycle/state machine beyond `isActive` is desired; verify no partial implementation exists | Medium | Consolidated engine + shadow metrics | `docs/adr/ADR-009-deterministic-rule-engine-contract.md`, `src/lib/services/rule-precedence-engine.ts` |

## How to use this index

1. Take a candidate.
2. Follow the **Sources**.
3. Run the **Verification Action** (confirms, redefines, or discards items).
4. Build `01-explore.md` exclusively from confirmed evidence.

This keeps the candidate → source → verification → explore flow explicit and repeatable, per ADR-004 (evidence over assumptions).

## Verification report

The evidence, verification commands, and per-candidate results behind this index are recorded in [`change-candidate-index-verification-2026-08.md`](change-candidate-index-verification-2026-08.md).
